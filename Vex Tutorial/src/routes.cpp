#include "functions.h"
#include "main.h"

using namespace pros;

void example_route(){
    // Your basic functions:
    drive_dist(0);
    turn(0);
    drive_time(0);
    stop_motors();
    delay(0);
    
    // auton goes here
}